on: push
jobs:
  build:
    runs-on: ubuntu-22.04
    name: script/cibuild
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7.8'
          bundler-cache: true
      - name: test
        run: script/cibuild
